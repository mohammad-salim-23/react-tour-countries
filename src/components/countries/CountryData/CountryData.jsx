

const CountryData = ({country}) => {
    console.log(country)
    return (
        <div>
            <p><small>Country Data:{country}</small></p>
        </div>
    );
};

export default CountryData;